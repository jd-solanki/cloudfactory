{
  "name": "aisubs-gw",
  "version": "0.1.0",
  "private": true,
  "description": "Hosted aisubs: a permanent OpenAI-compatible base URL backed by a ChatGPT (Codex) subscription.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "dry-run": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "push-credential": "node scripts/push-credential.mjs"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.3.7",
    "aisubs": "^0.3.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260903.1",
    "typescript": "^5.9.2",
    "wrangler": "^4.129.0"
  }
}
